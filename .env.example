# 真實機票／住宿價格（選用）。在 Vercel 專案 → Settings → Environment Variables 設定。
# 不設定時，網站照常用「示範估價」（依市場行情模擬），完全不影響運作。
#
# 這些是「伺服器端」變數（給 /api/* serverless functions 用），不會打包進前端、不會外洩。
# 本機 npm run dev 不會跑 serverless，所以本機一律顯示示範估價；正式環境（Vercel）才會生效。

# 選擇 provider：travelpayouts 或 amadeus（留空＝停用，用示範估價）
PRICE_PROVIDER=

# ---- Travelpayouts（一個帳號 = 機票 Aviasales + 飯店 Hotellook + Booking/Agoda/Trip 分潤）----
# 註冊：https://www.travelpayouts.com/  → 取得 API token
TRAVELPAYOUTS_TOKEN=
TRAVELPAYOUTS_MARKER=

# ---- Amadeus Self-Service（官方免費 tier，真實機票/飯店 offer）----
# 註冊：https://developers.amadeus.com/  → 建立 App 取得 API Key / Secret
AMADEUS_CLIENT_ID=
AMADEUS_CLIENT_SECRET=
AMADEUS_ENV=test

# ---- 城市社群評論／照片 + 旅遊優惠券（選用）----
# 在 Vercel 專案 → Storage → 建立 KV（Upstash Redis，免費）即會自動注入下列兩個變數。
# 設定後：評論／照片變成「所有訪客共享」的社群；優惠券改為「站方管理的即時優惠」。
# 未設定時：評論存使用者本機、優惠券顯示示範券（連到合作夥伴官方優惠頁）。
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ---- 優惠券後台管理金鑰（選用）----
# 設一組你自己的密碼字串；之後用 POST /api/coupons 並帶 header `x-admin-token: <此值>`
# 即可新增即時優惠券到 KV。未設定時無法經 API 新增（只讀示範券）。
COUPONS_ADMIN_TOKEN=

# ---- 廣告（選用，前端變數，需 VITE_ 前綴）----
# 你的 Google AdSense 發布商 ID（例如 ca-pub-1234567890123456）。
# 設定後城市頁/目的地頁的版位自動顯示 AdSense 廣告；未設定時顯示乾淨的版位佔位。
VITE_ADSENSE_CLIENT=
